@contextScopeItemMention đọc api, và nghiên cứu luồng của customer, giờ tôi muốn tích hợp api vào ui rồi,, hãy hướng dẫn tôi từng bước cho từng trang ( tôi muốn sử dụng zustand cho global state, dùng usestate cho local component hay localpage,dùng react query cho việc gọi api đơn giản, dùng useMEMO cho trường hợp cần) hãy phân tích từng trang của customer và tạo file hướng dẫn tôi từng bước( gồm tạo state như nào, quản lý ra sao, tạo cây thư mục như nào, v.v tất tần tật việc của một FE dev cơ bản) , để chất lượng thì làm tạo file hướng dẫn cho 5 trang của customer đầu tiên đi ( login signup forgetpassword, trang tìm kiếm chuyến bay, trang kết quả tìm kiếm( nhớ lưu ý luồng để xem thử data nào là global state dể dùng zútand, local dunbgf usestate bình thường, nhớ để ý luồng logic gọi api cho tối ưu lần gọi nhất có thể nhé.